MongoDB Setup:
    mongodb + compas installiert

    in compas connection erstellt und neue datenbank "reisebüro" erstellt

    xml file in json umgewandelt und in reisebüro importiert

Nodejs Setup:
    npm init -y
    npm install express mongodb dotenv

    .env erstellt
    server.js erstellt

    node server.js